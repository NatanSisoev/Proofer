# Copy to .env.local to enable real AI problem generation + free-form grading.
# Without it, Proofer runs in "demo mode" (templated problems, length-based grading).
# Get a key at https://console.anthropic.com/  (or run `ant auth login`).
ANTHROPIC_API_KEY=sk-ant-...
